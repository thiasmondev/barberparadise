export async function runCampaignManager(): Promise<{ status: string; message: string }> {
  return {
    status: "pending",
    message: "Module Campaigns en cours de développement pour une phase ultérieure.",
  };
}
