export async function runImageGenerator(): Promise<{ status: string; message: string }> {
  return {
    status: "pending",
    message: "Module Images en cours de développement pour une phase ultérieure.",
  };
}
